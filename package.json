{
  "name": "ayursutra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web-app install && npm --prefix web-app run build",
    "dev": "npm --prefix web-app run dev",
    "start": "npm --prefix web-app run preview"
  },
  "dependencies": {
    "typescript": "^7.0.2"
  }
}